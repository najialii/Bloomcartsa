export default function SARSymbol({ className = '' }: { className?: string }) {
  return (
    <img
      src="/Saudi_Riyal_Symbol.svg"
      alt="SAR"
      className={`inline-block ${className}`}
    />
  );
}
